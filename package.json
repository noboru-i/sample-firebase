{
  "name": "sample-firebase",
  "scripts": {
    "setup": "cd functions; npm install; cd -",
    "build": "cd functions; npm run build; cd -",
    "serve": "npm run build && firebase emulators:start"
  },
  "license": "ISC",
  "dependencies": {
    "firebase-tools": "^8.10.0"
  }
}
